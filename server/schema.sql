CREATE TABLE IF NOT EXISTS Messages (timestamp BIGINT PRIMARY KEY,
                                     world STRING,
                                     message STRING,
                                     user STRING);

